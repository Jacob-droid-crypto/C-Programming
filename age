#include<stdio.h>
int main()
{
    int age;
    printf("Enter your age:");
    scanf("%d",&age);
    if(age>=18)
    {
        printf("The person is eligible ");
        
    }
    else
    {
        printf(" The person is not eligible");
    }
    return 0;
}
